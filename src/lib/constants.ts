import { Locale } from "./enums/locale.enum";

export const LOCALE_COOKIE_NAME = "locale";
export const DEFAULT_LOCALE = Locale.en;

// breakpoint values are referenced from tailwind
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  "2xl": 1536,
};
